# 集成分支开发指南 — integration-day3

**分支**: `integration-day3`（基于 dev，已推送到远程）
**时间**: 2026-07-18 09:00-17:00
**团队**: PM×1 + FE×1 + BE-A/B/C×3 + DBA×1
**工作流**: 每个成员在本地开发 → 提交到 integration-day3 → 其他人拉取最新继续开发

---

## 一、合并依赖链

```
DBA（演示数据）──┐
                 ├─→ 所有人可以测试
BE-A（学习API）──┤
                 ├─→ FE 开发 VideoPlayer 进度上报
BE-B（考试统计）──┤
                 ├─→ FE 开发 Exam/Stats 页面
BE-C（RAG数据）───┤
                 └─→ PM 准备 RAG 演示
                  ↓
            FE（全部页面）
            依赖 BE-A + BE-B + DBA
```

### 合并顺序（5 个里程碑）

| 节点 | 时间 | 合并者 | 产出 | 解锁谁 |
|------|------|--------|------|--------|
| **M1** | 10:30 | DBA | 演示数据脚本 + 索引 | 所有人进入测试阶段 |
| **M2** | 11:00 | BE-A | learning API + Schema | FE 的 VideoPlayer 进度上报 |
| **M3** | 12:00 | BE-B | 考试批改 + 统计聚合 | FE 的 Exam/Stats 页面 |
| **M4** | 13:30 | BE-C | RAG 演示数据 + Postman | PM 准备 RAG 演示 |
| **M5** | 16:00 | FE | 6 个前端页面 | 全链路联调 |

---

## 二、节点前后各成员任务

### 节点 M1：DBA 合并（10:30）

#### 合并前（09:15-10:30）

| 成员 | 做什么 | 交付物 |
|------|--------|--------|
| **DBA** | 1. 连接 MySQL 分析现有索引<br>2. 添加缺失索引（`idx_enrollments_user_course` 等）<br>3. 编写 `scripts/generate_demo_data.py`<br>4. 本地执行脚本验证数据正确性 | `scripts/generate_demo_data.py` |
| **BE-A** | 阅读 `app/models/learning_record.py` 和 `app/models/learning_calendar.py`，理解表结构<br>设计 learning API 的 Schema 和 Service 接口 | 设计草稿 |
| **BE-B** | 阅读 `app/services/exam_service.py` 的 `submit_exam()` 方法<br>找到批改逻辑入口，标记可能的 bug 点 | 代码审查笔记 |
| **BE-C** | 启动 ChromaDB 容器，验证连接<br>准备 3-5 个 PDF 文档放入 `data/` 目录 | PDF 文件就绪 |
| **FE** | 1. 阅读 `frontend/src/api/` 目录，理解 Axios 封装<br>2. 创建 `CourseList.vue` 空壳框架（路由 + 模板）<br>3. 用 Element Plus 搭建卡片网格布局 | CourseList 空壳 |
| **PM** | 1. 启动全栈环境（Docker + 后端 + 前端）<br>2. 验证 69 条 API 可用<br>3. 在 GitHub 创建 Project 看板<br>4. 发送启动会纪要到群里 | 环境就绪 + 看板 |

#### DBA 合并操作

```bash
git checkout integration-day3
git pull origin integration-day3

# DBA 提交
git add scripts/generate_demo_data.py
# 如有模型文件改动（索引通过 Alembic）
git add ...
git commit -m "feat(db): 添加演示数据生成脚本与性能索引"
git push origin integration-day3
```

#### 合并后（10:30-12:00）

| 成员 | 做什么 |
|------|--------|
| **全员** | `git pull origin integration-day3` 拉取最新<br>`python scripts/generate_demo_data.py` 执行脚本填充演示数据 |
| **DBA** | 验证数据量 → 协助 PM 写测试脚本 |
| **BE-A** | 开始编写 learning API（Service + Router + Schema） |
| **BE-B** | 开始修复考试批改 bug |
| **BE-C** | 调用知识库构建 API 构建向量库 |
| **FE** | 接入真实 API（`GET /api/v1/courses`），让 CourseList 展示真实数据 |
| **PM** | 运行演示数据脚本，验证数据完整性，开始准备演示数据 |

---

### 节点 M2：BE-A 合并（11:00）

#### 合并前（10:30-11:00）

| 成员 | 做什么 | 交付物 |
|------|--------|--------|
| **BE-A** | 1. 创建 `app/schemas/learning.py`（ProgressUpdate schema）<br>2. 创建 `app/services/learning_service.py`（update_progress 方法）<br>3. 创建 `app/api/v1/learning.py`（POST /learning/progress 路由）<br>4. 在 `__init__.py` 挂载 learning 路由<br>5. 本地 Postman 测试 | learning 模块 |
| **DBA** | 监控数据库连接，确保刚加的索引不拖慢写入 | - |
| **BE-B** | 继续阅读 exam_service.py，确认批改逻辑所有分支 | - |
| **BE-C** | 上传 PDF → 调用知识库构建 API → 等待后台任务完成 | - |
| **FE** | CourseList.vue 接入真实 API + 分页 + 搜索 | CourseList 页面 |
| **PM** | 开始编写全链路测试脚本<br>准备 Postman 环境变量 | 测试脚本 |

#### BE-A 合并操作

```bash
git checkout integration-day3
git pull origin integration-day3   # 先拉 DBA 的提交

# BE-A 提交
git add app/schemas/learning.py \
        app/services/learning_service.py \
        app/api/v1/learning.py \
        app/api/v1/__init__.py
git commit -m "feat(learning): 添加学习进度上报 API"
git push origin integration-day3
```

#### 合并后（11:00-12:00）

| 成员 | 做什么 |
|------|--------|
| **全员** | `git pull origin integration-day3` |
| **BE-A** | 验证视频 Range 流 → 协助 FE 接入 VideoPlayer |
| **BE-B** | 继续修复考试批改，开始补充测试用例 |
| **BE-C** | 验证知识库检索效果 |
| **FE** | **开始 VideoPlayer.vue**（进度上报 API 已可用） |
| **DBA** | 检查 learning_records 索引是否支持高频写入 |
| **PM** | Postman 测试 learning API → 通知 FE "API 就绪" |

---

### 节点 M3：BE-B 合并（12:00）

#### 合并前（09:15-12:00，含上午的累积工作）

| 成员 | 做什么 | 交付物 |
|------|--------|--------|
| **BE-B** | 1. 修复 `submit_exam()` 批改逻辑<br>2. 优化 `get_calendar()`/`get_duration()` 数据格式<br>3. 编写 `tests/test_exam_submit.py`<br>4. Postman 测试全部 exam + stats 接口 | exam 批改修复 + stats 优化 + 测试 |
| **BE-A** | 验证视频流 Range 请求正常 | 验证报告 |
| **BE-C** | 知识库构建完成 → 测试 SSE 流式问答 | 问答验证通过 |
| **FE** | CourseList 基本完成 → CourseDetail 开发中 | CourseDetail 页面 |
| **DBA** | 协助 BE-B 分析 exam_records 表写入性能 | - |
| **PM** | 验收 BE-A 的 learning API<br>在群里报告 M1+M2 完成状态 | 验收记录 |

#### BE-B 合并操作

```bash
git checkout integration-day3
git pull origin integration-day3   # 先拉 DBA + BE-A 的提交

# BE-B 提交
git add app/services/exam_service.py \
        app/services/stats_service.py \
        tests/test_exam_submit.py
git commit -m "fix(exam): 修复批改逻辑并优化统计数据格式"
git push origin integration-day3

# 运行全量测试验证
conda run -n edu_platform pytest -q
```

#### 合并后（12:00-13:30，含午休）

##### 12:00-12:30 快速同步

| 成员 | 做什么 |
|------|--------|
| **全员** | `git pull origin integration-day3` |
| **FE** | **开始 ExamList.vue + ExamPage.vue**（exam API 已验证可用） |
| **BE-B** | 确认测试全部通过 → 协助 FE 理解 API 返回格式 |
| **PM** | Postman 测试 exam + stats 全部接口 → 输出接口文档给 FE |

##### 12:30-13:30 午休

---

### 节点 M4：BE-C 合并（13:30）

#### 合并前（11:00-13:30）

| 成员 | 做什么 | 交付物 |
|------|--------|--------|
| **BE-C** | 1. 知识库构建完成<br>2. 编写 `scripts/test_sse.py`<br>3. 导出 `docs/postman/RAG-Demo.postman_collection.json`<br>4. 准备演示话术草稿 | 测试脚本 + Postman 集合 + 话术 |
| **FE** | VideoPlayer 开发中（进度上报联调） | VideoPlayer 页面 |
| **BE-A** | 协助 FE 调试视频播放 + 进度上报 | - |
| **BE-B** | 协助 FE 调试考试提交 + 统计图表 | - |
| **DBA** | 检查数据完整性，确保演示数据覆盖所有场景 | - |
| **PM** | 验收 BE-B 的考试批改<br>联系 FE 确认前端进度 | 验收记录 |

#### BE-C 合并操作

```bash
git checkout integration-day3
git pull origin integration-day3

# BE-C 提交
git add scripts/test_sse.py \
        docs/postman/
git commit -m "feat(rag): 添加 SSE 测试脚本与 RAG 演示 Postman 集合"
git push origin integration-day3
```

#### 合并后（13:30-17:00）

| 成员 | 做什么 |
|------|--------|
| **全员** | `git pull origin integration-day3` |
| **FE** | **全力冲刺所有前端页面**（详见第五节） |
| **BE-A/B/C** | **全部转为协助 FE 联调**（3 个后端对 1 个前端） |
| **DBA** | 准备演示环境数据库 + 监控连接池 |
| **PM** | 主导联调流程 + 记录 Bug |

---

### 节点 M5：FE 合并（16:00）

#### 合并前（13:30-16:00）

| 成员 | 做什么 |
|------|--------|
| **FE** | 由 3 个后端协助，完成 6 个页面 + 联调 |
| **BE-A** | 协助 VideoPlayer + 进度上报联调 |
| **BE-B** | 协助 Exam + Stats 联调 |
| **BE-C** | 协助整体路由跳转 + Element Plus 样式 |
| **DBA** | 检查数据一致性，备份当前数据库 |
| **PM** | 每 30 分钟检查 FE 进度，记录阻塞点 |

#### FE 合并操作

```bash
git checkout integration-day3
git pull origin integration-day3

# FE 提交所有前端文件
git add frontend/src/pages/CourseList.vue \
        frontend/src/pages/CourseDetail.vue \
        frontend/src/pages/VideoPlayer.vue \
        frontend/src/pages/ExamList.vue \
        frontend/src/pages/ExamPage.vue \
        frontend/src/pages/StatsPage.vue \
        frontend/src/router/index.js
git commit -m "feat(frontend): 完成 6 个核心业务页面"
git push origin integration-day3
```

#### 合并后（16:00-17:00）

| 成员 | 做什么 |
|------|--------|
| **全员** | `git pull origin integration-day3` |
| **PM** | 主导全链路端到端测试<br>注册→登录→选课→播放→考试→统计 |
| **DBA** | 协助 Docker Compose 部署 |
| **BE-A/B/C** | 修复全链路测试中发现的 Bug |
| **FE** | 修复前端 Bug |

---

## 三、每个成员完整任务清单（按时间）

### PM（全时段协调 + 验收）

```
09:00 启动会（15分钟）
09:15 启动 Docker + 验证环境
      → docker compose up -d mysql redis chromadb
      → conda run -n edu_platform uvicorn app.main:app --reload &
      → cd frontend && npm run dev &
      → curl http://localhost:8000/health
      → curl http://localhost:5173
09:30 验证 69 条 API（逐一检查）
      → curl http://localhost:8000/api/v1/courses | python -m json.tool
10:00 创建 GitHub Project 看板
10:30 ← M1: 拉取 DBA 提交 → 执行演示数据脚本
11:00 ← M2: 拉取 BE-A 提交 → Postman 测试 learning API
11:30 通知 FE "进度上报 API 就绪"
12:00 ← M3: 拉取 BE-B 提交 → Postman 测试 exam + stats
12:30 午休
13:30 ← M4: 拉取 BE-C 提交 → 准备 RAG 演示
14:00 开始全链路测试脚本
15:00 每30分钟检查 FE 进度
16:00 ← M5: 拉取 FE 提交 → 全链路端到端测试
16:30 主导 Docker 部署
17:00 演示彩排
```

### FE（前端页面开发）

```
09:15 阅读 API 封装 → 创建 CourseList.vue 空壳
10:00 搭建 Element Plus 卡片网格布局（Mock 数据）
10:30 ← 拉取 DBA 提交 → 执行演示数据 → 接入真实数据
11:00 ← 拉取 BE-A 提交 → 开始 VideoPlayer.vue
12:00 ← 拉取 BE-B 提交 → 开始 ExamList + ExamPage
12:30 午休
13:30 VideoPlayer 完成 → 进度上报联调
14:00 ExamList + ExamPage 完成
15:00 StatsPage 完成（复用 ECharts 示例）
15:30 全页面自查 + 路由测试
16:00 提交所有文件 → M5 合并
```

### BE-A（学习进度 API + 视频流）

```
09:15 阅读 learning_record 模型 → 设计 Schema
10:00 编写 learning_service.py
10:30 ← 拉取 DBA 提交 → 执行演示数据
10:30 创建 learning API 路由 → 挂载 → Postman 测试
11:00 提交 → M2 合并
11:00 验证视频 Range 流
12:00 协助 FE 调试 VideoPlayer
12:30 午休
13:30 继续协助 FE 联调
16:00 全链路测试 + Bug 修复
```

### BE-B（考试批改 + 统计优化）

```
09:15 阅读 exam_service.py → 标记批改逻辑
10:00 阅读 stats_service.py → 检查数据格式
10:30 ← 拉取 DBA 提交 → 执行演示数据 → 用真实数据测试
11:00 修复批改逻辑 + 编写测试
12:00 提交 → M3 合并
12:00 协助 FE 理解 exam/stats 返回格式
12:30 午休
13:30 协助 FE 调试 ExamPage + StatsPage
16:00 全链路测试 + Bug 修复
```

### BE-C（RAG 演示准备）

```
09:15 启动 ChromaDB → 验证连接
10:00 准备 3-5 个 PDF 文档
10:30 ← 拉取 DBA 提交 → 构建知识库
11:00 测试 SSE 流式问答
12:00 编写 test_sse.py → 准备 Postman 集合
12:30 午休
13:30 提交 → M4 合并
14:00 协助 FE 调试整体样式
16:00 全链路测试 + Bug 修复
```

### DBA（数据库优化 + 测试数据）

```
09:15 连接 MySQL → 分析索引
09:30 添加缺失索引
10:00 编写 generate_demo_data.py
10:30 提交 → M1 合并
10:30 协助 PM 执行数据脚本
11:00 监控连接池 + 慢查询
12:00 检查数据完整性
12:30 午休
13:30 备份数据库
14:00 准备 Docker 环境
16:00 协助 PM 部署
17:00 演示彩排
```

---

## 四、合并操作规范

### 每个人合并前的检查清单

```bash
# 1. 切换到集成分支
git checkout integration-day3

# 2. 拉取最新代码（重要！）
git pull origin integration-day3

# 3. 确认无冲突
git status

# 4. 运行全量测试（后端改动者）
conda run -n edu_platform pytest -q

# 5. 提交自己的改动
git add <改动的文件>
git commit -m "<type>(<scope>): <描述>"

# 6. 推送
git push origin integration-day3

# 7. 如有冲突，在本地解决后再推送
git pull origin integration-day3   # 拉取+合并
# 解决冲突...
git add .
git commit -m "merge: 解决冲突"
git push origin integration-day3
```

### 群内通知模板

```
@所有人
[M1/M2/M3/M4/M5] 已合并到 integration-day3

合并内容：
- xxx
- xxx

新增文件：
- xxx

请立即执行 git pull origin integration-day3 拉取最新代码。

下一步：
- FE 可以开始开发 xxx
- BE-x 协助联调 xxx
- PM 准备验收 xxx
```

---

## 五、FE 页面开发详细指引

### 每个页面对应的 API 与 Element Plus 组件

| 页面 | API | EL 组件 | video.js | ECharts |
|------|-----|---------|----------|---------|
| CourseList | GET /api/v1/courses | Table, Pagination, Input | - | - |
| CourseDetail | GET /api/v1/courses/:id<br>GET /api/v1/courses/:id/chapters<br>POST/DELETE .../enroll | Card, Collapse, Button, Tag | - | - |
| VideoPlayer | GET /chapters/:id/courseware<br>POST /learning/progress | TreeSelect, Button | ✅ | - |
| ExamList | GET /api/v1/exams | Card, Button, Tag | - | - |
| ExamPage | POST /exams/:id/start<br>POST /exams/:id/submit<br>GET /exams/:id/result | RadioGroup, Checkbox, Button | - | - |
| StatsPage | GET /stats/calendar<br>GET /stats/duration | Card, Select | - | ✅ Calendar |

### FE 路由配置（需更新）

```js
// frontend/src/router/index.js
const routes = [
  { path: '/login', name: 'login', component: () => import('@/pages/Login.vue'), meta: { public: true } },
  { path: '/register', name: 'register', component: () => import('@/pages/Register.vue'), meta: { public: true } },
  { path: '/', name: 'home', component: () => import('@/pages/Home.vue') },
  { path: '/courses', name: 'courses', component: () => import('@/pages/CourseList.vue') },        // 新增
  { path: '/courses/:id', name: 'courseDetail', component: () => import('@/pages/CourseDetail.vue') }, // 新增
  { path: '/learn/:coursewareId', name: 'learn', component: () => import('@/pages/VideoPlayer.vue') },  // 新增
  { path: '/exams', name: 'exams', component: () => import('@/pages/ExamList.vue') },               // 新增
  { path: '/exams/:id', name: 'examPage', component: () => import('@/pages/ExamPage.vue') },        // 新增
  { path: '/stats', name: 'stats', component: () => import('@/pages/StatsPage.vue') },              // 新增
]
```

---

## 六、成功标准

### M1（10:30）完成标准
- [ ] 演示数据可一键生成（`python scripts/generate_demo_data.py`）
- [ ] 索引已添加
- [ ] 所有人都拉到 M1 代码并执行了数据脚本

### M2（11:00）完成标准
- [ ] `POST /api/v1/learning/progress` 可正常调用
- [ ] Postman 测试返回 200
- [ ] FE 可以接入进度上报

### M3（12:00）完成标准
- [ ] exam submit 批改逻辑正确
- [ ] `tests/test_exam_submit.py` 新增测试通过
- [ ] stats 返回数据格式与 ECharts 兼容

### M4（13:30）完成标准
- [ ] 知识库已构建（3+ PDF 文档）
- [ ] SSE 流式问答可用
- [ ] Postman 集合已导出

### M5（16:00）完成标准
- [ ] 6 个前端页面全部完成
- [ ] 路由跳转正常
- [ ] API 数据正确渲染

### 最终演示（17:00）完成标准
- [ ] 全链路流程无阻塞
- [ ] Docker Compose 一键启动
- [ ] 演示彩排通过

---

**立即开始执行！**
