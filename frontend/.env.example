# === 开发模式（Vite proxy 代理，推荐）===
# Vite 已配置 /api 和 /uploads 代理到 localhost:8000。
# 使用相对路径，所有请求同源 → 无 SameSite cookie 问题。
VITE_API_BASE_URL=/api/v1

# 启用本地 mock 数据（true=独立演示 / false=连真实后端）
VITE_USE_MOCKS=false
